# Headway — Status 2026-05-12

## Current phase: T4+T5 — Appearance (Theme + Typography) ✅ COMPLETED TODAY

### What was built today

Implemented **Phase T4 — Theme toggle** (dark / light / sepia / system) and
**Phase T5 — Typography settings** (font size + line height) in one combined stage.

---

## Changes made to `index.html` (~130 lines added, now ~7 134 lines total)

| Area | What changed |
|---|---|
| `:root` CSS | Added `--reading-font-size: 18px` and `--reading-line-height: 1.7` CSS variables |
| CSS (theme overrides) | Added `[data-theme=dark]`, `[data-theme=light]`, `[data-theme=sepia]` override blocks |
| `.chapter-body` | Changed hard-coded `font-size: 18px; line-height: 1.7` to `var(--reading-font-size, 18px)` / `var(--reading-line-height, 1.7)` |
| `.markdown` | Same CSS variable switch (affects summaries, feed posts, mindmap text) |
| CSS (new) | `.theme-picker`, `.theme-btn`, `.theme-btn.active`, `.typo-row`, `.typo-btn.active` styles |
| Settings HTML | New "Appearance" section at the top: 4 theme buttons + 4 font-size buttons + 4 line-height buttons |
| Build tag | Phase T4+T5 Appearance · `build 2026-05-12` |
| JS (new functions) | `applyTheme()`, `applyTypography()`, `_updateThemePicker()`, `_updateTypoBtns()`, `applyStoredAppearance()` |
| `boot()` | Calls `await applyStoredAppearance()` after `openDB()` so theme applies on every load |
| `openSettings()` | Populates Appearance fields from IDB on open |
| `bindUI()` | Theme picker click, font-size button clicks, line-height button clicks, `prefers-color-scheme` change listener |

### Theme palette

| Theme | `--bg` | `--text` | Character |
|---|---|---|---|
| Dark (default) | `#0b1220` | `#e2e8f0` | Deep navy, for night reading |
| Light | `#ffffff` | `#111827` | Clean white, high contrast |
| Sepia | `#f4ece0` | `#3d2b1f` | Warm parchment, easy on eyes |
| System | — | — | Follows `prefers-color-scheme` |

### Typography options

| Setting | Options |
|---|---|
| Font size | Small (15px) / **Medium (18px)** / Large (21px) / Huge (24px) |
| Line height | Compact (1.4) / **Normal (1.7)** / Relaxed (1.95) / Loose (2.2) |

---

## Verified in browser

- App boots without console errors
- Build tag reads `build 2026-05-12`
- Settings → Appearance section visible at top; Dark + Medium + Normal are default-active
- Clicking Light → entire app switches to white theme instantly ✅
- Clicking Sepia → warm parchment theme applied instantly ✅
- Clicking Dark → back to deep navy ✅
- Clicking Large font → `--reading-font-size` set to `21px` instantly ✅
- Close settings → settings saved to IDB
- Reload → theme (dark) and font size (21px) restored from IDB ✅
- Zero JS errors throughout

---

## State of the codebase

### Architecture
Still **pre-Phase-G single-file** architecture. ~7 134 lines in `index.html`.

### Phases completed

| Phase | Description | Status |
|---|---|---|
| A | Foundation (IDB, library, chapter view, PWA) | ✅ |
| B | AI Core (Gemini, summary, flashcards, quiz, teach-back) | ✅ |
| C | Mind Map, Socratic, chapter/book Ask | ✅ |
| D | Feed system (8 personalities, deep-dive writeups) | ✅ |
| E | TTS + persistent audio player (browser TTS, Lazybird) | ✅ |
| F | Google Drive cloud sync | ✅ |
| J | Advanced quizzes (Quiz Hub, Speed Round, Fill Blanks, Connections) | ✅ |
| K | Multi-Provider AI (Merlin, Junia, Perplexity, DocAnalyzer stub) | ✅ |
| L | Discovery Module (3-step pipeline, IDB stores, sub-tabs, 24h cache) | ✅ |
| T1 | Command Palette Search (⌘K, book+chapter full-text, highlight, navigate) | ✅ |
| T4+T5 | Appearance — dark/light/sepia/system theme + font-size + line-height | ✅ TODAY |

---

## Recommended next stage

**Phase T2 — Reading Progress Persistence (scroll position memory)**

The IDB `settings` store can hold per-chapter scroll offsets. Tasks:
1. On chapter open → restore scroll position if saved, show "Resume?" toast/button.
2. On scroll (throttled to 1 write per 2s) → `setSetting('reading_pos_<chapterId>', { offset, ts })`.
3. Mark-complete button already wired — just needs the visual completion bar polish.

Runner-up options:
- **Phase N2 — In-text annotations** (highlight text → contextmenu → color + note). High value for power users. More complex (~200 lines).
- **Phase N3 — Spaced repetition** (SM-2 review deck surfaced in library). Complex but very impactful for learning.
- **Phase N8 — Streak heatmap** (GitHub-style 52×7 SVG calendar). Pure rendering, self-contained, ~80 lines.
- **Phase N11 — Library sort options** (dropdown: Recently added / Alphabetical / Completion %). Very quick win, ~30 lines.

---

## Known gaps / follow-ups

1. `.markdown` (summaries, feed) font size now follows `--reading-font-size`; heading sizes inside markdown are relative (`em`) so they scale correctly.
2. The sepia `--accent` colour (#fbbf24 amber) looks good on sepia background — no conflict.
3. "System" theme correctly reads `prefers-color-scheme` and re-applies on OS change.
4. All known gaps from Phase T1 remain (generated content not indexed in search, etc.).

## How tomorrow's agent should start

1. Read **this file** first.
2. Sanity-check: open `?nosw=1`; build tag → `build 2026-05-12`; open Settings → Appearance section present.
3. **Top recommendation: Phase T2 — Reading position memory** (~60 lines, self-contained, high UX value).
4. **Runner-up: Phase N8 — Streak heatmap** (pure SVG rendering, ~80 lines, no IDB changes needed beyond reading `progress` store already populated).
5. **Runner-up: Phase N11 — Library sort options** (~30 lines, very quick win).
6. Add a new `docs/status-2026-05-13.md` summarising the day.

## Files changed today

- `/Users/oskarsokolov/NEWWAY/index.html` — +~130 lines (Phase T4+T5 CSS, HTML, JS)
- `/Users/oskarsokolov/NEWWAY/docs/status-2026-05-12.md` — this file
