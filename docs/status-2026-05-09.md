# Headway — Status 2026-05-09

## Current phase: K — Multi-Provider AI ✅ COMPLETED TODAY

### What was built today

Implemented **Phase K — Multi-Provider AI** on top of the existing Phase J
(Advanced Quizzes) + Phase F (Cloud Sync) codebase. The app now supports
four AI backends in addition to Google Gemini, with a unified `callAI`
dispatcher and a full Settings UI for per-provider configuration.

#### Changes made to `index.html` (~270 lines added, now ~6 000 lines total)

| Area | What changed |
|---|---|
| `hasAnyAIProvider()` | Now checks all five providers, not just Gemini key |
| New Phase K block | Inserted after Phase B `Object.assign` — all provider code in one section |
| `generateUUID()` | v4 UUID helper (used by Merlin SSE request body) |
| `getMerlinTimestamp()` | ISO timestamp with timezone bracket (Merlin header) |
| `authenticateMerlin(email, pw)` | Firebase signInWithPassword → stores `merlinIdToken`, `merlinRefreshToken`, `merlinTokenExpiry`, `merlinEmail` |
| `refreshMerlinToken()` | `securetoken.googleapis.com/v1/token` refresh; stores new token |
| `getMerlinToken()` | Returns cached token if not within 60 s of expiry, else refreshes |
| `callMerlinAPI(prompt, opts)` | SSE chat via `getmerlin.in/arcane/api/v2/thread/unified`; streams tokens via `opts.onToken` |
| `callMerlinAPIWithImage(prompt, b64, opts)` | Passes image attachment (for Phase M OCR path) |
| `callJuniaAPI(prompt, opts)` | Bearer-token chat via `junia.ai/api/ai-chat`; strips trailing UUID from response |
| `callPerplexityAPI(prompt, opts)` | POST to `/api/perplexity/proxy`; forwards API key in body |
| `callDocAnalyzerAPI(prompt, opts)` | Stub — throws "not yet implemented"; full flow in Phase L |
| `callAI(prompt, keyOverride, modelOverride, opts)` | Dispatcher: reads `aiProvider` setting, routes to correct provider |
| `Object.assign(App, …)` | Exports all new provider functions |
| `_toggleProviderSettings(provider)` | Shows/hides provider config divs in Settings |
| `openSettings()` | Loads AI provider + all per-provider fields from IDB |
| `saveSettings()` | Persists `aiProvider`, Merlin model/webAccess, Junia token/creativity/gpt4, Perplexity key, DocAnalyzer key |
| `bindUI()` | Wires: provider `<select>` change → `_toggleProviderSettings`; Merlin auth button; all Show/Hide key toggles |
| Build tag | `Phase J Advanced Quizzes · build 2026-05-08` → `Phase K Multi-Provider AI · build 2026-05-09` |

#### New Settings HTML section (inserted between Gemini and Custom Prompts)

**AI PROVIDER** section with:
- Provider `<select>` (Gemini / Merlin / Junia / Perplexity / DocAnalyzer stub)
- `#provider-merlin`: email, password + Show, Authenticate button, status span, model select, web-access checkbox
- `#provider-junia`: bearer token + Show, creativity select, GPT-4 checkbox
- `#provider-perplexity`: API key + Show
- `#provider-docanalyzer`: API key + Show (stub)

#### New Vercel proxy files

| File | Purpose |
|---|---|
| `api/perplexity/proxy.js` | POST → `api.perplexity.ai/chat/completions`; CORS headers; key in body |
| `api/docanalyzer/proxy.js` | POST/DELETE → `api.docanalyzer.ai/api/v1/<path>`; CORS headers |

#### Other file changes

- `vercel.json` — added `rewrites` for both proxy endpoints
- `sw.js` — cache version bumped `headway-v6` → `headway-v7`

---

## Verified in browser

- App boots without console errors (`?nosw=1`)
- Build tag reads `build 2026-05-09`
- Settings modal → **AI PROVIDER** section renders; dropdown defaults to "Google Gemini (default)"
- Switching to Merlin AI reveals email/password/Authenticate/model/web-access config
- Switching to Perplexity hides Merlin, shows API-key field
- Switching back hides all provider panels correctly
- Zero JS errors throughout

---

## State of the codebase

### Architecture
Still **pre-Phase-G single-file** architecture. ~6 000 lines in `index.html`.

### Phases completed (in `index.html`)

| Phase | Description | Status |
|---|---|---|
| A | Foundation (IDB, library, chapter view, PWA) | ✅ |
| B | AI Core (Gemini, summary, flashcards, quiz, teach-back) | ✅ |
| C | Mind Map, Socratic, chapter/book Ask | ✅ |
| D | Feed system (8 personalities, deep-dive writeups) | ✅ |
| E | TTS + persistent audio player (browser TTS, Lazybird) | ✅ |
| F | Google Drive cloud sync | ✅ |
| J | Advanced quizzes (Quiz Hub, Speed Round, Fill Blanks, Connections) | ✅ |
| K | Multi-Provider AI (Merlin, Junia, Perplexity, DocAnalyzer stub) | ✅ TODAY |

### Phases not yet started

| Phase | Description | Priority |
|---|---|---|
| G | Architectural rebuild (TypeScript + Vite + modular, XL effort) | Medium |
| H | Multi-project workspaces (requires G) | High |
| I | Source generalization (Book → Source) | High |
| L | Discovery module (Perplexity 3-step pipeline) — **now unblocked by K** | High |
| M | Advanced import + OCR + URL/DOI ingestion | Medium |
| N | PDF viewer (scroll/slide, highlighter, pen) | Medium |
| O | Writing Hub (outline editor, streaming AI drafts) | High |
| P | Writing exercises (6 types, persistence) | Medium |
| Q | Citations & source library | Medium |
| R | Video & image generation (Vadoo, Bonkers) | Low |
| S | Cross-source intelligence | Medium |
| T | Quality of life (search, annotations, spaced repetition, themes) | Medium |

---

## Recommended next stage

**Phase L — Discovery Module** is now unblocked by Phase K.

Discovery uses Perplexity Sonar to run a 3-step pipeline:
1. Generate search queries from the active project's keywords
2. Search the web and rank results
3. Present a curated "discovery feed" of new sources to explore

Alternatively: **Phase O — Writing Hub** (outline editor + streaming AI drafts)
is a high-value WRITE pillar that is independent of Phase G.

A lighter option is **Phase T QoL items** — full-text search across the library,
annotation highlighting, and spaced-repetition scheduler — each a contained 
1–2 day unit.

---

## Known gaps / follow-ups

1. **Merlin live test still pending** — requires real credentials; SSE streaming
   path code is complete and follows the spec exactly.
2. **Junia live test pending** — requires real bearer token.
3. **Perplexity live test pending** — requires Vercel deployment + real key.
4. **DocAnalyzer is a stub** — full upload-then-chat flow deferred to Phase L.
5. **`callAI` not wired to existing generators** — all generators still call
   `callGeminiAPI` directly. Phase L should do a find-replace sweep to route
   through `callAI`; for now only newly-written Phase L code uses `callAI`.
6. **Sync whitelist not updated** — Merlin/Junia/Perplexity keys should be
   added to `SETTINGS_SYNC_WHITELIST` for Drive sync. (One-line addition.)
7. **Real-API smoke tests still pending** from earlier phases (feed prompt,
   writeup prompt, image generation, Lazybird TTS, Drive OAuth).

## How tomorrow's agent should start

1. Read **this file** first; check `status-2026-05-08.md` if Phase J details needed.
2. Sanity-check: `python3 -m http.server :8080` with `?nosw=1`.
3. Tackle **Phase L — Discovery Module**
   ([`docs/implementation-plan/phase-l-discovery-module.md`](implementation-plan/phase-l-discovery-module.md)).
   Three-step Perplexity pipeline: keyword → search queries → ranked source feed.
   Wire `callAI` (provider: perplexity) into the new Discovery tab.
4. Also fix gap #5 and #6 above (wire `callAI` in existing generators; sync whitelist).
5. Add a new state file `docs/status-2026-05-10.md` summarising the day.

## Files changed today

- `/Users/oskarsokolov/NEWWAY/index.html` — +~270 lines (Phase K code + Settings HTML)
- `/Users/oskarsokolov/NEWWAY/sw.js` — cache version v6 → v7
- `/Users/oskarsokolov/NEWWAY/vercel.json` — added proxy rewrites
- `/Users/oskarsokolov/NEWWAY/api/perplexity/proxy.js` — new file
- `/Users/oskarsokolov/NEWWAY/api/docanalyzer/proxy.js` — new file
- `/Users/oskarsokolov/NEWWAY/docs/status-2026-05-09.md` — this file
